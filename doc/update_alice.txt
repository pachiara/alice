Aggiornamento

1) Alice
    login con user = alice
    salvataggio attuale:
    tar czvf alice.`date +%d%m%Y`.tgz alice/
    rm -rf alice
    git clone https://github.com/pachiara/alice.git
    cd alice
    bundle install
    rake db:migrate RAILS_ENV=production
    rake tmp:clear
    rake assets:clean
    rake assets:precompile RAILS_RELATIVE_URL_ROOT=/alice

2) Apache + Passenger

   login con user = root
   riavviare apache2:
   apache2ctl restart