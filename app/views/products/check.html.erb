<%= form_for @product, url: {action: 'update_check'}, html: {method: "post"} do |f| %>

  <%= hidden_field_tag 'product_id', @product.id %>
  <%= hidden_field_tag 'result', @product.result %>
  <%= hidden_field_tag 'compatible_license_id', (!@product.compatible_license.nil? ? @product.compatible_license.id : " ") %>

  <%= render 'check_result.html' %>

  <div class="form-group form-horizontal">

    <div class="form-group">
      <%= button_tag(type: 'submit', class: "btn btn-primary") do %>
        <span class="glyphicon glyphicon-ok glyphicon-inverse"></span> <%= t('actions.save') %>
      <% end %>
      <% if !@product.result.nil? %>
        <%= link_to product_print_check_path(@product, :format => :pdf), :class => "btn btn-default" do %>
          <span class="glyphicon glyphicon-print"></span> <%= t('actions.print') %>
        <% end %>
      <% end %>
      <%= link_to products_path, :class => "btn btn-default" do %>
          <span class="glyphicon glyphicon-arrow-left"></span> <%= t('actions.back') %>
      <% end %>
    </div>

  </div>

<% end %>
