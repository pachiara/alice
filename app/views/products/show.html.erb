<script>
  var width = 480,
      height = 250,
      radius = Math.min(width, height) / 2;
  var arc = d3.svg.arc()
      .outerRadius(radius - 10)
      .innerRadius(radius - 60);
  var donut_font_size = "18px";
      
  var legend_font_size = "14px";
  var legend_translate = 20;
  var legend_rect_side = 18;
  var legend_dy = ".35em"
  var legend_text_x = 24;
  var legend_text_y = 9;
  var legend_width = 188;
</script>
<%= simple_form_for(@product) do |f| %>

  <div class="form-inputs">

    <%= render 'graphics' %>

  <div class="form-actions">
    <%= link_to products_path, :class => "btn" do %>
      <i class="icon-arrow-left"></i> <%= t('actions.back') %>      
    <% end %> 
    <%= link_to product_print_path(@product, :format => :pdf), :class => "btn" do %>
      <i class="icon-print"></i> <%= t('actions.print') %>      
    <% end %> 
  </div>
<% end %>